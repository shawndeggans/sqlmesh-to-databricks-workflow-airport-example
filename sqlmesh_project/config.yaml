# SQLMesh Configuration for Airport Inventory Service
# Two gateways: local (DuckDB) and databricks_dev

gateways:
  local:
    connection:
      type: duckdb
      database: ../data/dev.duckdb
    state_connection:
      type: duckdb
      database: ../data/state.duckdb

  databricks_dev:
    connection:
      type: databricks
      server_hostname: "{{ env_var('DATABRICKS_HOST') }}"
      http_path: /sql/1.0/warehouses/5f2a600257bdb5b8
      access_token: "{{ env_var('DATABRICKS_TOKEN') }}"
      catalog: airport_inventory_dev
    # Using DuckDB for state since Databricks is not optimized for transactional workloads
    state_connection:
      type: duckdb
      database: ../data/databricks_dev_state.duckdb

default_gateway: local

model_defaults:
  dialect: duckdb
  start: '2024-01-01'

# Linting Rules
linter:
  enabled: true
  rules:
    - ambiguousorinvalidcolumn
    - invalidselectstarexpansion
    - noambiguousprojections
